CC := $(CROSS_COMPILE)gcc
CFLAGS := -Wall -Werror -g

TARGET := writer

SRC := finder-app/writer.c
OBJ := $(SRC:.c=.o)

all: $(TARGET)

$(TARGET): $(SRC)
	$(CC) $(CFLAGS) -o $@ $^

clean:
	rm -f $(TARGET) $(OBJ)
